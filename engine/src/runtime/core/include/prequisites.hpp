#pragma once

#include <string>
#include <memory>
#include <vector>
#include <map>

namespace PrimalDawn {
    // 类声明在这里，其他类依赖的头文件就不用写了
    class AbstractObject;
    class Camera;
    class Engine;
    class Entity;
    class Light;
    class Material;
    class Mesh;
    class Meshpart;
    class Node;
    class Viewport;
    class Scene;
    class SceneManager;
    class SceneNode;


    typedef std::string String;
}